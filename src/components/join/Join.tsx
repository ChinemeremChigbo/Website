import React from 'react';
import './Join.scss';

export default function Join() {
  //Temporary inline styling. Should be changed when real code is added
  return (
    <div className="join" style={{ position: 'absolute', top: 100, left: 50 }}>
      Join us
    </div>
  );
}
