import React from 'react';
import './About.scss';

export default function About() {
  //Temporary inline styling. Should be changed when real code is added
  return (
    <div className="about" style={{ position: 'absolute', top: 100, left: 50 }}>
      About us
    </div>
  );
}
