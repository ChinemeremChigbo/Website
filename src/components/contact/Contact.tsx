import React from 'react';
import './Contact.scss';

export default function Contact() {
  //Temporary inline styling. Should be changed when real code is added
  return (
    <div className="contact" style={{ position: 'absolute', top: 100, left: 50 }}>
      Contact us
    </div>
  );
}
