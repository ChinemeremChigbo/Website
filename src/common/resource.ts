interface Resource {
  [key: string]: string;
}
const resource: Resource = {
  // home page text
  HOME_TITLE: 'This is BP',

  // nav bar text

  // contact text

  // about page text
};

export default resource;
